# Test memory operands: store 42 to stack, load it back
_start:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov rax, 42
    mov [rbp - 8], rax
    mov rdi, [rbp - 8]
    mov rax, 60
    syscall
