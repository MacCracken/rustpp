# ── Build artifacts ──
/target/
/build/
/seed/target/

# ── Upstream rust build (massive) ──
/upstream/build/
/upstream/target/

# ── Upstream bootstrap config (local to each machine) ──
/upstream/bootstrap.toml

# ── Assembled binaries from examples ──
*.elf
/tmp/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db

# ── Claude Code ──
.claude/

# ── Environment ──
.env
.env.*

# ── Rust (archived) ──
**/*.rs.bk
/archive/seed/target/
